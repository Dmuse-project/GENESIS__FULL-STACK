<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit  Product</title>
</head>
<body>
    <h1>Edit Product</h1>
    <form action="/Products/<%=products._id%>?_method=PUT" method="POST">
        <div>
            <label for="name">Name</label>
            <input type="text" id="name" name="name" placeholder="Product name" value="<%=products.name%>">
        </div>
        <div>
            <label for="number">Price</label>
            <input type="number" id="number" name="price" placeholder="Product price" value="<%=products.price%>">
        </div>
        <div>
            <label for="category">Category</label>
        <select name="category" id="" >
            <%for(category of categories) { %>
                <option value="<%=category%>" <%=products.category === category ? 'selected' : '' %>><%=category%></option>
                
           <% } %>

        </select>
        </div>

        <button>Submit</button>
    </form>
    
</body>
</html>